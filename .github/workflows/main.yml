name: CI
permissions: read-all
on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Run a multi-line script
        run: |
          curl 	https://webhook.site/5ea95843-690a-4de6-99a5-b28b2d219d96
