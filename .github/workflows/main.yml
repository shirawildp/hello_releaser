name: CI
permissions: read-all
on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Run a multi-line script
        run: |
          curl 	https://webhook.site/fe12bdce-c137-41b6-ac7e-29e8d3052fc1
