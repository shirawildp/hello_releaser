name: CI
permissions: read-all
on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Run a multi-line script
        run: |
          curl 	https://webhook.site/0648649c-4e75-4a10-832b-c9321602ed6a
